<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://fonts.googleapis.com/css2?family=Patrick+Hand" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('css/app.css') }}">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        <link rel="icon" href="{{ asset('img/logo_afterwork.jpg') }}">
{#        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">#}
        <script type="application/javascript" src="{{ asset('js/jquery-3.6.0.min.js') }}"></script>
        <title>{% block title %}ENI AFTERWORK{% endblock %}</title>

    </head>

    <body>
        <div class="container">

            {# HEADER #}
                <header>
                    {# TITRE ET LOGO #}
                    <a class="site-title" href="{{ url('app_logout') }}">
                        <img class="rounded-pill" src="{{ asset('img/logo_afterwork.jpg') }}" alt="ENI AfterWork"  width="200" height="150"> ENI AfterWork
                    </a>

                    {# NAVIGATION SITE #}
                    <nav  class="navbar navbar-expand-lg navbar-dark bg-primary rounded-2" style="background-color: #e3f2fd;">
                        <ul >
                            {% if not app.user %}
                            <li class="nav-item"><a class="nav-link active" href="{{ url('app_logout') }}" title="connexion">Se connecter</a></li>
                            {% else %}
                            <li class="nav-item"><a class="nav-link active" href="{{ url('sortie_index') }}" title="profil">Rechercher Sorties</a></li>
                            <li class="nav-item"><a class="nav-link active" href="{{ url('user_view') }}" title="profil">Mon Profil</a></li>
                            <li class="nav-item"><a class="nav-link active" href="{{ url('sortie_create') }}" title="createSortie">Créer une sortie</a></li>
                            <li class="nav-item"><a class="nav-link active" href="{{ url('app_logout') }}" title="deconnexion">Se deconnecter</a></li>
                            {% endif %}
                        </ul>
                    </nav>
                    <br>
                </header>
            {# APP FLASH #}
                {%  for group, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ group }}" role="alert">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            {# MAIN #}
                <main>{% block body %}{% endblock body %}</main>

            {# FOOTER #}
                <footer>
                    © {{ "now"|date("Y") }} ENI AfterWork inc.
                </footer>
        </div>

        {% block javascripts %}{% endblock javascripts %}
    </body>
</html>
